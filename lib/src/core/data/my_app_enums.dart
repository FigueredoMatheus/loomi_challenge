enum TextFormFieldType {
  password,
  confirmPassword,
  email,
  name,
}

enum SocialMediaType {
  google,
  apple,
}

enum ImageSourceType {
  camera,
  gallery,
}

enum SnackBarType {
  textFieldError,
}
